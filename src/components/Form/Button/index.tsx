import React from 'react';

import { Container, Title } from './styles';

const Button: React.FC = () => {
  return (
    <Container>
      <Title>Button</Title>
    </Container>
  );
}

export default Button;